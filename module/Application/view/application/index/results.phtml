<?php 
	$techItems = $this->technologyItems;
	$tech = $this->technology;
	$cTech = count($tech);
	
	$tagItems = $this->tagItems;
	$tag = $this->tag;
	$cTag = count($tag);
?>

<div class="wrapp">

<?php 
if ($cTech) {
    echo "<div class='ccbox mainwrapper' style='margin-bottom:5px;'>";
    	echo "<h2>Einträge für die Technologie '<strong>{$tech->getName()}</strong>'</h2>";
      	
      	foreach($techItems as $k) {
				echo "<div data-link='" . $this->url('item', array('id' => $k->getId())) . "' class='recommendations-wrapper left'>";
				echo "<div class='recommendation'>";
				
				echo "<p>" . $k->getContent() . "</p>";
				
				echo "</div></div>";
		}
      	      	
    echo "</div>";
}
?>

<?php 
if ($cTag) {
    echo "<div class='ccbox mainwrapper'>";
    	echo "<h2>Einträge mit dem Tag '<strong>{$tag->getName()}</strong>'</h2>";
      	foreach($tagItems as $k) {
			echo "<div data-link='" . $this->url('item', array('id' => $k->getId())) . "' class='recommendations-wrapper left'>";
			echo "<div class='recommendation'>";
			
			echo "<p>" . $k->getContent() . "</p>";
			
			echo "</div></div>";
		}
      	      	
    echo "</div>";
}
?>
</div>